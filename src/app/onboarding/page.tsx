import { OnboardingFlow } from '@/components/modules/onboarding/onboarding-flow';

export default function OnboardingPage() {
  return (
    <main className="min-h-screen forge-grid">
      <OnboardingFlow />
    </main>
  );
}
